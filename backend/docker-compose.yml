services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: catops
      POSTGRES_USER: catops
      POSTGRES_PASSWORD: localdev
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
